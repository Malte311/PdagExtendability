version: "2"

services:
  pdag_extensions:
    restart: unless-stopped
    #image: 
    container_name: pdag_extensions
    volumes:
      - ./configs/:/configs/
      - ./src/:/src/
    #environment: